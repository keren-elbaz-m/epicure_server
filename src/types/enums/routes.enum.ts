export enum RoutePath {
    ROOT = "/",
    API = "/api",
    HEALTH = "/health",
    RESTAURANTS = "/restaurants",
    CHEF = "/chef",
    DISH = "/dish",
    BREAKFAST = "/breakfast",
    LUNCH = "/lunch",
    DINNER = "/dinner",
    UPLOAD = "/upload",
}
